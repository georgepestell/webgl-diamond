# WebGL Diamond

A set of simple WebGL canvases creating renders and animations using proper matrix calculations from the ground up.

- `p1.html` contains a basic diamond object rendered with textures applied and ambient lighting.
- `p2.html` adds animation and other textured objects with a directed light source.

## Running

A makefile is provided which runs the python http web server:

```bash
make
```
